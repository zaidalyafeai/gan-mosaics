version: 0.1
entrypoint: python runway_model.py
python: 3.6
cuda: 10.0
framework: tensorflow
spec:
  gpu: True
  cpu: False
files:
  ignore:
    - checkpoints/*
build_steps:
  - pip install -r requirements.txt